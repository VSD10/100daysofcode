def getNthFromLast(head,n):
    temp=head
    a=0
    while temp is not None:`
        a+=1
        temp=temp.next
        
    if a>=n:
        temp=head
        for _ in range(a-n):
            temp=temp.next
        return temp.data
    else:
